# Vite envs (read at build time)
# For local dev, prefer .env.development or .env.local
VITE_API_URL=http://localhost:8000/api

# Backward compatibility:
# VITE_API_BASE_URL=http://localhost:8000/api

# Optional base path if app is hosted under a sub-path.
VITE_BASE_PATH=/
